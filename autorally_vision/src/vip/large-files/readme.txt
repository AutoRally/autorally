The contents of /large-files are where rosbags will go,
 as well as where the training data will be stored locally.
These have been excluded from git intentionally.

To get access to the box with rosbags, ask one of the owners of the org.